/* ---------------------------------
Simple:Press
User Selection Plugin Javascript
------------------------------------ */
(function(spj, $, undefined) {
	spj.setUserOption = function(opt) {
		spj.cookie(opt.name, opt.value, {expires: 365, path: '/'});
		window.location.reload();
	};
}(window.spj = window.spj || {}, jQuery));
